export async function notifyTrustedContacts(userId: string, sosId: string): Promise<void> {
  // Internship-project notification placeholder.
  // SMS/email provider can be connected here later without changing the SOS controller.
  console.log(`[SilentSOS] notification queued for user=${userId}, sos=${sosId}`);
}
